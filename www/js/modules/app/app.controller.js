(function () {
    'use strict';

    var appController = angular.module('app.controller', []);

    appController.controller('appController', ['$state', '$scope', function ($state, $scope) {

        // Main controller, probably won't be used for anything just yet

    }]);

})();
